pipeline {
   agent any 
     stages {
      stage ('build'){
        steps {
         echo "build"
         echo "Run"
         }
        }

      stage('Dev') {
        steps {
         echo "Build"
         echo "Run"
	}
       }
      }
     }

